from setuptools import setup, find_packages

setup(name='edw-pycaptcha',
      version='0.3',
      author='Eau de Web',
      author_email='office@eaudeweb.ro',
      packages=find_packages(),
      zip_safe=False,
)
