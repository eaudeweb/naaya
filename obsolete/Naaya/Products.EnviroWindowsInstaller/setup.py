from setuptools import setup, find_packages

setup(
    name='Products.EnviroWindowsInstaller',
    version='1.1.2',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
